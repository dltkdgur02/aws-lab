name: CI

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: 코드 가져오기
        uses: actions/checkout@v4
      - name: 파일 확인
        run: ls -al
      - name: 로그인 파일 검사
        run: test -f login.md && echo "login.md OK"